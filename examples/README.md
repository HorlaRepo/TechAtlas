# Examples

Keep first-run assets here: a safe seed-domain CSV, sample queries/responses, demo data, and eventually a Postman/Bruno collection. Examples must never include secrets, private customer data, or unlicensed datasets.

