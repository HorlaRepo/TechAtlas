# Cross-service tests

Use this directory for integration, contract, and reusable fixture assets. Keep unit tests colocated with their implementation where that gives clearer ownership.

# Test fixtures

`fixtures/imports/mixed-domains.csv` covers an existing-domain duplicate, normalized accepted domain, invalid domain, and an in-file duplicate for the CSV import integration tests.
